{
  "name": "@reimagined/github-actions-publish",
  "license": "MIT",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc --build && ncc build lib/index.js --license licenses.txt",
    "clean": "tsc --clean",
    "add-dist": "git add --force dist"
  },
  "dependencies": {
    "@actions/core": "1.2.5"
  }
}

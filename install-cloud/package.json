{
  "name": "@reimagined/github-actions-install-cloud",
  "license": "MIT",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf dist && ncc build src/main.ts -o dist/main --license lic.txt",
    "test": "jest --coverage --config=../jest.config.js",
    "clean": "rm -rf node_modules coverage 2>/dev/null || true",
    "add-dist": "git add --force dist"
  },
  "dependencies": {
    "@actions/core": "1.2.5",
    "semver": "^7.3.4",
    "lodash.clonedeep": "4.5.0"
  },
  "devDependencies": {
    "@types/lodash.clonedeep": "4.5.6",
    "ts-jest": "26.5.1",
    "jest": "26.6.3"
  }
}

{
  "name": "@reimagined/github-actions-deploy-cloud",
  "license": "MIT",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts --license licenses.txt --target es2015",
    "clean": "tsc --clean",
    "add-dist": "git add --force dist"
  },
  "dependencies": {
    "@actions/core": "1.2.5"
  }
}
